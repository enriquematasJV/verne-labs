/*
 * GraphDisplay — Componente para cualquier tipo de gráfico (canvas o SVG)
 *
 * Proporciona estilos y comportamiento común para gráficos en cualquier laboratorio.
 * No contiene lógica de rendering, solo contenedor y estilos.
 *
 * Uso:
 *   <div class="graph-display">
 *     <canvas id="graph"></canvas>
 *   </div>
 *
 *   O:
 *   <div class="graph-display">
 *     <svg id="graph"></svg>
 *   </div>
 */

// Este componente es puramente CSS-driven
// No requiere JavaScript
